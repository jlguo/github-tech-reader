# Cloudflare Pages deployment config
# 在 Cloudflare Dashboard → Workers & Pages 中配置以下内容：

name = "cloudshelf"
compatibility_date = "2025-06-16"
pages_build_output_dir = "frontend/dist"

# 构建环境变量（在 Cloudflare Dashboard 中设置）：
# VITE_API_BASE_URL = https://你的服务名.onrender.com/api
